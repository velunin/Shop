@model DeleteFromCartModel

@using (Html.BeginForm("Delete", "Cart", FormMethod.Post))
{
    @Html.HiddenFor(m => m.ProductId)
    @Html.HiddenFor(m => m.ReturnUrl)
    <input type="submit" value="Delete" class="btn btn-primary"/>
}
